@using System.Web.Optimization

@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ASP_Blog Admin</title>

    @Styles.Render("~/admin/styles")
</head>
<body>
   
    <div class="container">
        <ul class="nav nav-pills">
            <!--title of link Posts, action is Index, controller is Posts, and area is admin-->
            <li>@Html.ActionLink("Posts", "Index", "Posts", new { area = "admin" }, new {}) </li>
            <li>@Html.ActionLink("Users", "Index", "Users", new { area = "admin" }, new {})</li>
            <li class="pull-right">@Html.RouteLink("Back to Website", "Home")</li>
        </ul>
        <!--INSERT VIEW HERE!-->
        @RenderBody()
    </div
    @Scripts.Render("~/admin/scripts")
    @RenderSection("Scripts", false)
</body>
</html>


